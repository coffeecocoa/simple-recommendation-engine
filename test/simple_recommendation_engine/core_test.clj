(ns simple-recommendation-engine.core-test
  (:require [clojure.test :refer :all]
            [simple-recommendation-engine.core :refer :all]))

(deftest a-test
  (testing "FIXME, I fail."
    (is (= 0 1))))
